# DI 统计工具 — 设计方案

## 一、系统概述

**项目名称：** DI 统计工具
**项目路径：** `E:\aitest\bug-di-analysis`
**技术栈：** Python + Streamlit + pandas + requests

**核心功能：**
1. 爬取两个目标网页的问题单数据
2. 合并数据并按 DI 规则统计
3. 按云服务/微服务分层展示
4. 支持问题单明细查看和 Excel 导出

---

## 二、项目结构

```
E:\aitest\bug-di-analysis\
├── app.py                    # Streamlit 主页面
├── config.py                 # 配置管理（认证信息）
├── crawler.py                # 爬虫模块
├── processor.py              # 数据处理模块
├── di_calculator.py          # DI 统计计算模块
├── styles.py                 # Streamlit 样式配置
├── requirements.txt          # 依赖列表
└── README.md                 # 使用说明
```

---

## 三、认证配置（config.py）

**设计思路：** 运行时输入，不写死在代码里，不保存到磁盘。

**Streamlit 侧边栏输入：**
- Cookie（密码输入框）
- Authorization Token（密码输入框）
- x-titan-userid（文本输入框）

---

## 四、爬虫模块（crawler.py）

### 4.1 接口分析

| 接口 | URL | 方法 | 用途 |
|------|-----|------|------|
| 触发导出 | `/vision-excel/api/export/issue/v2` | POST | 提交导出任务 |
| 查询状态 | `/vision-excel/api/query/file_download_record` | POST | 查询文件是否就绪 |
| 下载文件 | `/vision-excel/api/query/issue/download_item` | GET | 下载 Excel 文件 |

### 4.2 两个 URL 的处理

**URL1 条件（有 assigned_domain 过滤）：**
- 用于获取特定域的问题单

**URL2 条件（无 assigned_domain 过滤）：**
- 用于获取更全面的问题单

### 4.3 核心流程

```
触发导出(POST) → 获取 file_id → 轮询查询状态(POST) → 文件就绪后下载(GET)
```

---

## 五、数据处理模块（processor.py）

### 5.1 字段映射

| 字段 | 英文字段名 |
|------|-----------|
| 严重程度 | `severity_level` |
| 问题状态 | `status` |
| 责任服务 | `assigned_to_domain` |
| 发现问题版本 | `from_version` |
| 发现迭代 | `discover_iteration` |
| 问题创建时间 | `created_time` |
| 交付场景 | `delivery_scenario` |
| 挂起/撤销 | `valid` |
| 发现环境 | `discovered_environment` |
| 标签 | `labels` |
| 研发责任人 | `dev_person` |
| 测试责任人 | `testOwners` |
| 问题单号 | `number` |
| 问题标题 | `title` |

### 5.2 合并去重

- 合并两个数据源
- 按问题单号（`number`）去重

---

## 六、DI 统计计算（di_calculator.py）

### 6.1 合格标准

| 层级 | DI 阈值 | 合格标准 |
|------|---------|---------|
| 云服务 | < 20 | 合格 |
| 微服务 | < 5 | 合格 |

### 6.2 CES 微服务列表（模糊匹配）

- CES告警、CES公共开发、CES、CES监控、CES Agent、CES API、CES测试、CES资料、CES运维变更、CES-Console

### 6.3 DI 权重

| 严重程度 | DI 权重 |
|---------|--------|
| 致命 | 10 |
| 严重 | 3 |
| 一般 | 1 |
| 提示 | 0.1 |

### 6.4 SLA 阈值

| 严重程度 | 阈值（天） |
|---------|-----------|
| 致命 | 7 |
| 严重 | 14 |
| 一般/提示 | 30 |

### 6.5 CCB 挂起处理

| 环境 | 规则 |
|------|------|
| 非生产环境 | 挂起 → **剔除** |
| 生产环境 | 挂起 → **正常参与** |

### 6.6 生产环境 DI 计算

**前置条件：** 发现环境 = "生产环境"

| 问题状态 (status) | 问题阶段 (stage) | 是否统计 DI | 说明 |
|------------------|-----------------|------------|------|
| 待验收 | 空 | ❌ 不统计 | - |
| 已关闭 | 空 | ❌ 不统计 | - |
| 修复 | 修复完成 | ❌ 不统计 | - |
| 待确认 | 空 | ✅ 统计 | 按SLA超期计算 |
| 待修复 | 空 | ✅ 统计 | 按SLA超期计算 |
| 修复 | 修复中 | ✅ 统计 | 按SLA超期计算 |
| 修复 | 修复测试 | ✅ 统计 | 按SLA超期计算 |

**SLA 超期计算：** 当前时间 - 发现时间 > SLA阈值 → 算 DI

**SLA 阈值：**
| 严重程度 | 阈值（天） |
|---------|-----------|
| 致命 | 7 |
| 严重 | 14 |
| 一般/提示 | 30 |

### 6.7 非生产环境 DI 计算

**前置条件：** 发现环境 = "非生产环境"

| 问题状态 (status) | 问题阶段 (stage) | 是否统计 DI | 说明 |
|------------------|-----------------|------------|------|
| 待提交 | 空 | ❌ 不统计 | - |
| 已关闭 | 空 | ❌ 不统计 | - |
| 待确认 | 空 | ✅ 统计 | 按SLA超期计算 |
| 待修复 | 空 | ✅ 统计 | 按SLA超期计算 |
| 修复 | 修复中 | ✅ 统计 | 按SLA超期计算 |
| 修复 | 修复测试 | ✅ 统计 | 按SLA超期计算 |
| 修复 | 修复完成 | ✅ 统计 | 按SLA超期计算 |

**SLA 超期计算：** 当前时间 - 发现时间 > SLA阈值 → 算 DI

### 6.8 交付场景

| 字段值 | 门禁 DI |
|--------|---------|
| 华为云/伙伴云/HCSO | ✅ 算 |
| HCS | ❌ 不算 |
| 空 | ✅ 正常算 |

### 6.9 发现问题版本为空处理

- `from_version` 为空的问题单 → 分散计入每个有值的版本
- 不作为独立过滤选项

---

## 七、页面布局（app.py）

```
┌─────────────────────────────────────────────────────────────┐
│  DI 统计工具                                                │
├──────────────┬──────────────────────────────────────────────┤
│  认证配置     │                                              │
│  ─────────   │   云服务 DI 概览                              │
│  Cookie:     │   ┌────────────────────────────────────┐    │
│  [输入框]     │   │  云服务名: Cloud Eye                │    │
│              │   │  总DI值: xxx  │  总问题单: xxx      │    │
│  Token:      │   │  合格: ✅/❌                        │    │
│  [输入框]     │   └────────────────────────────────────┘    │
│              │                                              │
│  User ID:    │   ─────────────────────────────────────     │
│  [输入框]     │   发 现 问 题 版 本 过 滤                    │
│              │   [全部] [版本A] [版本B] [版本C] ...          │
│  [刷新数据]   │   当前选中：全部                             │
│              │   ─────────────────────────────────────     │
│  ─────────   │   CES 微服务 DI 明细（基于当前版本过滤）       │
│              │   ┌────────────────────────────────────┐    │
│  导出原始数据  │   │ 表格（可排序）                       │    │
│  [导出Excel]  │   │ 微服务 │ DI值 │ 问题单 │ 合格 │ 操作│    │
│              │   │ ...    │ ...  │ ...   │ ...  │ 查看│    │
│  导出统计结果  │   └────────────────────────────────────┘    │
│  [导出Excel]  │                                              │
│              │   问题单明细（点击"查看"后展开）              │
│  ─────────   │   ┌────────────────────────────────────┐    │
│              │   │ 表格（可排序）                       │    │
│  当前版本:全部│   │ 问题单号 │ 严重程度 │ ... │ 链接  │    │
│              │   └────────────────────────────────────┘    │
└──────────────┴─────────────────────────────────────────────┘
```

### 7.1 过滤按钮逻辑

| 点击按钮 | 显示内容 |
|---------|---------|
| **全部** | 所有问题单 |
| **版本A** | `from_version = 版本A` + `from_version 为空`的问题单 |

### 7.2 问题单跳转链接

```
https://clouddevops.huawei.com/#/bug/ + 问题单号
```

---

## 八、错误处理

| 场景 | 处理方式 |
|------|---------|
| 认证失败（401/403） | 提示用户检查认证信息 |
| 导出超时 | 提示"导出时间过长，请重试" |
| 文件下载失败 | 提示"下载失败，请重试" |
| Excel 解析失败 | 提示"数据格式错误"并打印原始数据 |
| 网络断开 | 提示"网络连接失败"并提供重试按钮 |

---

## 九、数据流全景图

```
用户输入认证信息
        ↓
点击「刷新数据」
        ↓
┌───────────────────────────────────────┐
│  爬虫模块                              │
│  ├─ URL1: trigger_export → file_id   │
│  ├─ URL1: wait_and_download → df1    │
│  └─ URL2: trigger_export → file_id   │
│       URL2: wait_and_download → df2  │
└───────────────────────────────────────┘
        ↓
┌───────────────────────────────────────┐
│  数据处理模块                           │
│  ├─ load_excel() → DataFrame         │
│  ├─ merge_data(df1, df2)              │
│  └─ normalize_columns()               │
└───────────────────────────────────────┘
        ↓
┌───────────────────────────────────────┐
│  DI 计算模块                           │
│  ├─ filter_by_ccb() → 过滤挂起        │
│  ├─ calculate_production_di() → 计算DI│
│  ├─ apply_delivery_scenario() → 交付场景│
│  └─ calculate_microservice_di() → 分组 │
└───────────────────────────────────────┘
        ↓
┌───────────────────────────────────────┐
│  Streamlit 页面展示                     │
│  ├─ 云服务概览卡片                      │
│  ├─ 版本过滤按钮（基于 from_version）   │
│  ├─ 微服务 DI 表格（可排序）            │
│  └─ 问题单明细表格（可跳转）            │
└───────────────────────────────────────┘
```

---

## 十、依赖

```
streamlit
pandas
openpyxl
requests
```

---

*设计方案版本：v1.0*
*日期：2026-03-27*
