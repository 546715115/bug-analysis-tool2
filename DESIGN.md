# DI 统计工具 — 设计方案

## 一、系统概述

**项目名称：** DI 统计工具
**项目路径：** `E:\aitest\bug-di-analysis`
**技术栈：** Python + Streamlit + pandas + requests + streamlit-aggrid + openpyxl + plotly

**核心功能：**
1. 支持 API 导入和 Excel 导入两种数据获取方式
2. 合并数据并按 DI 规则统计
3. 按云服务/微服务分层展示
4. 按版本过滤问题单
5. 三图表分析（月度分布柱状图 + 严重程度饼图 + 环境分布饼图）
6. 支持问题单明细查看和 Excel 导出

---

## 二、项目结构

```
E:\aitest\bug-di-analysis\
├── app.py                    # Streamlit 主页面
├── config.py                 # 配置管理（认证信息、请求头）
├── crawler.py                # 爬虫模块（API 导入）
├── processor.py              # 数据处理模块
├── di_calculator.py          # DI 统计计算模块
├── styles.py                 # Streamlit 样式配置
├── requirements.txt           # 依赖列表
└── README.md                 # 使用说明
```

---

## 三、数据导入（app.py）

### 3.1 API 导入

- Cookie（密码输入框）
- Authorization Token（密码输入框）
- x-titan-userid（文本输入框）
- Domain ID 列表（支持多个，用逗号分隔，默认 11, 33921）

### 3.2 Excel 导入

- 支持 3 个 Excel 文件上传（至少上传 1 个）
- 自动合并多个 Excel 文件并按问题单号去重
- 支持中文列名到英文字段名的转换

### 3.3 侧边栏布局

- 侧边栏默认折叠，左上角有 ☰ 展开按钮
- API 导入和 Excel 导入默认折叠
- 导入成功后自动折叠并刷新页面
- 导出功能：导出版本有效DI-Excel（dialog 弹窗选择条件）

---

## 四、爬虫模块（crawler.py）

### 4.1 接口分析

| 接口 | URL | 方法 | 用途 |
|------|-----|------|------|
| 触发导出 | `/vision-excel/api/export/issue/v2` | POST | 提交导出任务 |
| 查询状态 | `/vision-excel/api/query/file_download_record` | POST | 查询文件是否就绪 |
| 下载文件 | `/vision-excel/api/download/workitem?id={file_id}` | GET | 下载 Excel 文件 |

### 4.2 两个导出条件

**条件1（有 assigned_domain 过滤）：**
- `with_assigned_domain` - 用于获取特定域的问题单

**条件2（无 assigned_domain 过滤）：**
- `without_assigned_domain` - 用于获取更全面的问题单

### 4.3 核心流程

```
触发导出(POST) → 获取 file_id → 轮询查询状态(POST) → 文件就绪后下载(GET)
```

---

## 五、数据处理模块（processor.py）

### 5.1 字段映射

| 英文字段名 | 中文列名（支持多变体） |
|-----------|---------------------|
| number | 问题单号、问题编号 |
| title | 标题、问题标题 |
| severity_level | 严重程度 |
| status | 问题状态、状态 |
| stage | 问题阶段 |
| assigned_to_domain | 责任服务、负责域 |
| from_version | 发现问题版本、版本 |
| discover_iteration | 发现迭代 |
| created_time | 创建时间 |
| discovered_time | 发现时间 |
| delivery_scenario | 交付场景 |
| valid | 挂起/撤销 |
| discovered_environment | 发现环境 |
| labels | 标签 |
| dev_person | 研发责任人 |
| testOwners | 测试责任人 |

### 5.2 合并去重

- 合并两个数据源（with_assigned_domain + without_assigned_domain）
- 按问题单号（`number`）去重

### 5.3 版本过滤逻辑

- version="全部"：返回所有数据
- version 有值：返回 `from_version <= 选中版本` 或 `from_version 为空` 的数据

---

## 六、DI 统计计算（di_calculator.py）

### 6.1 合格标准

| 层级 | DI 阈值 | 合格标准 |
|------|---------|---------|
| 云服务 | < 20 | DI < 20 且所有微服务 DI < 5 |
| 微服务 | < 5 | 单个微服务 DI < 5 |

### 6.2 CES 微服务列表（模糊匹配）

CES告警、CES公共开发、CES、CES监控、CES Agent、CES API、CES测试、CES资料、CES运维变更、CES-Console

### 6.3 DI 权重

| 严重程度 | DI 权重 |
|---------|--------|
| 致命 | 10 |
| 严重 | 3 |
| 一般 | 1 |
| 提示 | 0.1 |

### 6.4 SLA 阈值

| 严重程度 | 阈值（天） |
|---------|-----------|
| 致命 | 7 |
| 严重 | 14 |
| 一般/提示 | 30 |

### 6.5 CCB 挂起处理

| 环境 | 规则 |
|------|------|
| 非生产环境 | 挂起 → **剔除** |
| 生产环境 | 挂起 → **正常参与** |

### 6.6 生产环境 DI 统计规则

| 问题状态 (status) | 问题阶段 (stage) | 是否统计 DI |
|------------------|-----------------|------------|
| 待提交 | 空 | ❌ |
| 待验收 | 空 | ❌ |
| 已关闭 | 空 | ❌ |
| 修复 | 修复完成 | ❌ |
| 待确认 | 空 | ✅ |
| 定位中 | 空 | ✅ |
| 待修复 | 空 | ✅ |
| 修复 | 修复中 | ✅ |
| 修复 | 修复测试 | ✅ |

### 6.7 非生产环境 DI 统计规则

| 问题状态 (status) | 问题阶段 (stage) | 是否统计 DI |
|------------------|-----------------|------------|
| 待提交 | 空 | ❌ |
| 已关闭 | 空 | ❌ |
| 待确认 | 空 | ✅ |
| 定位中 | 空 | ✅ |
| 待修复 | 空 | ✅ |
| 修复 | 修复中 | ✅ |
| 修复 | 修复测试 | ✅ |
| 修复 | 修复完成 | ✅ |
| 待验收 | 空 | ✅ |

### 6.8 交付场景

HCS 交付场景不计入 DI。

### 6.9 发现问题版本为空处理

`from_version` 为空的问题单会计入每个有值的版本。

---

## 七、页面布局（app.py）

### 7.1 页面结构

```
┌─────────────────────────────────────────────────────────────────┐
│  📊 CES DI 统计工具                                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ☁️ 云服务 DI 概览                                                │
│  ┌──────────┬──────────┬──────────┬──────────┐                  │
│  │ 云服务    │ 总 DI 值  │ 总问题单  │ 合格标准❓│                  │
│  │ Cloud Eye │ xxx     │ xxx     │  ✅/❌   │                  │
│  └──────────┴──────────┴──────────┴──────────┘                  │
│                                    [📥 导出版本有效DI-Excel]       │
│                                                                   │
│  🔍 发 现 问 题 版 本                                              │
│  ┌──────────────────┐  ┌─────────────────────────────────┐      │
│  │ [全部/版本选择 ▼] │  │ 合格/不合格标记                  │      │
│  └──────────────────┘  └─────────────────────────────────┘      │
│  当前选中：xxx                                                  │
│  ═══════════════════════════════════════════════════════════   │
│                                                                   │
│  📊 图表分析                                                      │
│  ┌────────────┬────────────┬────────────┐                       │
│  │ 月度问题分布│ 严重程度分布 │ 环境分布    │                       │
│  │ 柱状图      │ 饼图        │ 饼图       │                       │
│  │ (3/2/1月)  │            │            │                       │
│  │ >3/ >6个月  │            │            │                       │
│  └────────────┴────────────┴────────────┘                       │
│  ─────────────────────────────────────────────────────────────  │
│                                                                   │
│  📋 CES 微服务 DI 明细                                            │
│  ┌─────────────────────────────────────────────────────────┐     │
│  │ 微服务名      │ DI 值    │ 问题单数 │ 是否合格            │     │
│  │ CES监控      │ 12.5    │ 8       │ ❌ 不合格           │     │
│  │ CES Agent    │ 3.2     │ 5       │ ✅ 合格             │     │
│  │ ...          │ ...     │ ...     │ ...                │     │
│  │ 总计         │ 15.7    │ 13      │ ❌ 不合格           │     │
│  └─────────────────────────────────────────────────────────┘     │
│  ─────────────────────────────────────────────────────────────  │
│                                                                   │
│  🔍 微服务查看问题单详情                                          │
│  [全部/CES监控 ▼]                                               │
│  ┌─────────────────────────────────────────────────────────┐     │
│  │ 问题单号 │ 问题单环境 │ 标题   │ 严重程度 │ 状态    │ 操作  │ │
│  │ 🔗 详情  │            │        │          │          │       │ │
│  └─────────────────────────────────────────────────────────┘     │
│  ⚙️ 字段选择器                                    ◀ 1/3 ▶       │
│  ─────────────────────────────────────────────────────────────  │
│                                                                   │
│  📄 问题单明细                                                    │
│  ┌─────────────────────────────────────────────────────────┐     │
│  │ 问题单号 │ 问题单环境 │ 标题   │ 严重程度 │ 问题状态 │ 操作  │ │
│  │ 🔗 详情  │            │        │          │          │       │ │
│  └─────────────────────────────────────────────────────────┘     │
│  ⚙️ 字段选择器                                    ◀ 1/5 ▶       │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘

侧边栏（默认折叠）:
┌─────────────────┐
│ ☰ 展开侧边栏    │  ← 主页面显示此按钮，侧边栏隐藏
└─────────────────┘

展开后侧边栏:
┌─────────────────┐
│ CES DI 统计工具  │
│                 │
│ [☰ 折叠]        │
│                 │
│ ▶ API 导入      │
│ ▶ 导入 Excel    │
│                 │
│ [导出版本有效DI-Excel] │
└─────────────────┘
```

### 7.2 图表分析

**月度问题分布（柱状图）：**

| 桶名 | 时间范围（当前2026年3月为例） |
|------|------------------------------|
| 3月（本月） | 2026-03-01 ~ 2026-03-31 |
| 2月（上月） | 2026-02-01 ~ 2026-02-28 |
| 1月（上上月） | 2026-01-01 ~ 2026-01-31 |
| >3个月 | 2025-12, 2025-11, 2025-10 |
| >6个月 | 2025-09 及更早 + discovered_time 为空 |

**严重程度分布（饼图）：** 按 `severity_level` 分组统计所有问题单数量。

**环境分布（饼图）：** 按 `discovered_environment` 分组统计所有问题单数量。

### 7.3 表格功能

#### 方案C：分支处理

**有 link_column 分支（需要"查看详情"链接）：**
- 使用 `st.dataframe` + `column_config.LinkColumn`
- 固定列宽：标题(large)、单号/版本(medium)、其他(small)
- 支持横向滚动
- 分页：页码选择器置于表格下方，右对齐

**无 link_column 分支（CES微服务DI明细）：**
- 使用 `st.dataframe` 响应式布局
- 数字列转字符串实现视觉对齐

### 7.4 字段选择器

- ⚙️ 按钮触发 `st.popover` 抽屉
- checkbox 列表按 `all_columns` 顺序展示
- "确认" / "恢复默认" 按钮
- 选择结果存储在 `session_state`

### 7.5 导出功能

- dialog 弹窗选择过滤条件
- 过滤条件：发现问题版本、CES 微服务
- 过滤逻辑：版本过滤 → 微服务过滤 → DI规则过滤
- 导出按问题单号升序排列
- 文件名格式：`发现问题版本{版本}-{微服务名}-{年/月/日/时/分}.xlsx`
- 导出列名为中文，与导入格式一致

### 7.6 问题单跳转链接

```
https://clouddevops.huawei.com/#/bug/{问题单号}
```

---

## 八、错误处理

| 场景 | 处理方式 |
|------|---------|
| 认证失败（401/403） | 提示用户检查认证信息 |
| 导出超时 | 提示"导出时间过长，请重试" |
| 文件下载失败 | 提示"下载失败，请重试" |
| Excel 解析失败 | 提示"数据格式错误"并打印原始数据 |
| 网络断开 | 提示"网络连接失败"并提供重试按钮 |

---

## 九、数据流全景图

```
用户选择导入方式
        ↓
┌───────────────────────────────────────┐
│  导入模块                               │
│  ├─ API导入: BugCrawler.fetch_data()   │
│  └─ Excel导入: load_excel()           │
└───────────────────────────────────────┘
        ↓
┌───────────────────────────────────────┐
│  数据处理模块                           │
│  ├─ merge_data() → 合并去重            │
│  ├─ normalize_columns() → 列名标准化    │
│  └─ filter_by_version() → 版本过滤     │
└───────────────────────────────────────┘
        ↓
┌───────────────────────────────────────┐
│  过滤模块                               │
│  ├─ filter_production_issues() → CCB挂起过滤│
│  └─ filter_by_di_rules() → DI>0过滤   │
└───────────────────────────────────────┘
        ↓
┌───────────────────────────────────────┐
│  DI 计算模块                           │
│  ├─ should_count_di() → 状态规则判断    │
│  ├─ calculate_di_for_issue() → 单问题DI │
│  ├─ calculate_cloud_di() → 云服务DI    │
│  └─ calculate_microservice_di_with_count() → 微服务DI│
└───────────────────────────────────────┘
        ↓
┌───────────────────────────────────────┐
│  Streamlit 页面展示                     │
│  ├─ 云服务概览卡片（合格标准❓弹窗）     │
│  ├─ 版本过滤下拉框                     │
│  ├─ 图表分析（柱状图+饼图x2）           │
│  ├─ CES微服务DI表格（st.dataframe）     │
│  ├─ 微服务问题单详情（st.dataframe+LinkColumn）│
│  └─ 问题单明细（st.dataframe+LinkColumn）│
└───────────────────────────────────────┘
        ↓
┌───────────────────────────────────────┐
│  导出模块                               │
│  ├─ 版本过滤 → 微服务过滤 → DI过滤     │
│  ├─ 按问题单号排序                     │
│  └─ export_to_excel() → 中文列名xlsx   │
└───────────────────────────────────────┘
```

---

## 十、依赖

```
streamlit>=1.28.0
streamlit-aggrid>=0.3.0
pandas>=2.0.0
openpyxl>=3.1.0
requests>=2.31.0
plotly>=5.0.0
```

---

## 十一、已知问题

### 月度图表总数不匹配

月度问题分布柱状图的总数量与严重程度饼图/环境分布饼图的总数量不一致。

**待排查方向：**
- 月度柱状图和严重程度饼图是否使用同一个数据源
- 是否存在其他过滤条件差异

---

*设计方案版本：v2.3*
*日期：2026-03-30*
*更新内容：同步所有已实现功能，图表分析、字段选择器、导出弹窗、st.dataframe分支处理等，章节结构对齐原版*
